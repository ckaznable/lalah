[workspace]
resolver = "3"
members = ["packages/lalah-host", "packages/lalah-vm", "packages/shared"]

# Release profile tuned for the realtime audio path.
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"

